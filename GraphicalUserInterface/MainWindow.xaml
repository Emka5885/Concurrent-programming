<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour" Height="auto" Width="auto">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Liczba kulek:"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0"/>

                <TextBox Width="80"
                 Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,10,0"/>

                <Button Content="Start"
                Width="100"
                Margin="0,0,8,0"
                Command="{Binding StartCommand}"/>

                <Button Content="Reset"
                Width="100"
                Command="{Binding ResetCommand}"/>
            </StackPanel>

            <UniformGrid Columns="4" Margin="0,8,0,0">
                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Status" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SimulationStatusText}"/>
                </StackPanel>

                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Pęd sumaryczny" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalMomentumText}"/>
                </StackPanel>

                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Energia kinetyczna" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalKineticEnergyText}"/>
                </StackPanel>
            </UniformGrid>

        </StackPanel>

        <Border Grid.Row="2"
            BorderThickness="4"
            BorderBrush="Green"
            Height="{Binding Height}"
            Width="{Binding Width}">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Blue" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </Grid>
</Window>